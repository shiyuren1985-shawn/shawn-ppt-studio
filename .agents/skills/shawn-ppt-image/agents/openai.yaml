interface:
  display_name: "Shawn-PPT-image"
  short_description: "带来源漂移检测与阶段交接的 PPT 成品候选管线"
  default_prompt: "使用 $shawn-ppt-image，按内容原始语言或我指定的语言完成内容预检；新运行封存源材料快照，恢复或扩页前检查漂移，候选就绪后生成可复制到新对话的正式交接。未指定版本的 8×1 默认走 Fast 8×1 Diversity；只有明确要求旧 Quick8 v5 或恢复 quick_8x1 时才走经典管线。4×3 默认走 Fast，只有明确要求时才走严格模式。"
